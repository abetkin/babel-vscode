'use strict';

var _require = require('chai');

var assert = _require.assert;

describe('User', function () {
  describe('action', function () {
    it('should term without error', function (done) {
      var a = 0;
      var b = a + 1;
      debugger;
      console.log(b);
      assert.ok(b);
      done();
    });
  });
});

//# sourceMappingURL=test.js.map